server:
  port: 9000
  compression:
    enabled: true
app:
  jwtSecret: JWTTestKey
  jwtExpirationInMs: 86400000

spring:
  datasource:
    url: jdbc:h2:mem:test;MODE=MySQL;INIT=CREATE SCHEMA IF NOT EXISTS APICHAT
    username: apichat
    password: temporal
  liquibase:
    contexts: test,
    default-schema: apichat
